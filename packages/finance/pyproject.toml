[project]
name = "finance-agent"
version = "0.1.0"
description = "金融领域包：工具实现、prompt、目标模板（loopbase 第一个领域消费者）"
requires-python = ">=3.14"
dependencies = ["loopbase"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
loopbase = { workspace = true }
