[project]
name = "loopbase"
version = "0.1.0"
description = "Lightweight loop-engineering state kernel for long-running agent teams"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
rust = []
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
