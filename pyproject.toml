[project]
name = "loopbase-workspace"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["loopbase", "finance-agent"]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
loopbase = { workspace = true }
finance-agent = { workspace = true }

[tool.ruff]
target-version = "py314"
