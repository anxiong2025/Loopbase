# Loopbase — 路线图

Stage 0-7 是内核要做的部分，Stage 8 是内核之外的编排层，暂不启动。整体定位、范围边界和设计原则见
[README.md](README.md)。

**实际推进顺序**：0 → 1 → 2.1-2.4 → **4** → (2.5 重新规划 / 3 多工具编排)。Stage 4 被提前是因为
2.5「根据新证据改计划」的输入就是证据、输出是计划修订版；证据层残缺时做 2.5，等于没有 diff 就改文件，
Stage 2 的完成标准也无法回放验证。

## 模块全景图

12 个模块层，每层标注对应哪个 Stage：

| # | 层 | 职责 | 对应 Stage |
|---|---|---|---|
| 1 | 核心循环控制层 | turn 执行器、终止判定、分支/并行控制原语、错误边界 | Stage 0-3 |
| 2 | 模型 I/O 层 | 多模型客户端抽象、结构化输出强制、流式增量拼装、transient 失败重试 | Stage 1 |
| 3 | 工具/动作层 | 工具注册表（含动态注册）、执行沙箱/权限边界、并发调度、结果归一化 | Stage 1、3 |
| 4 | 目标与任务管理层 | 结构化目标表示、任务分解与状态跟踪、重新规划触发条件 | Stage 2 |
| 5 | 上下文管理层 | 装配、预算跟踪、压缩、淘汰、缓存感知压缩 | Stage 5 |
| 6 | 记忆层 | 工作/情景/语义/程序性记忆、写入策略、冲突消解、检索 | Stage 5 |
| 7 | 状态持久层 | 事件日志、检查点/恢复协议、多会话身份隔离、状态 schema 版本化 | Stage 4 |
| 8 | 调度与生命周期层 | 配额跟踪、唤醒契约（外部调度器驱动）、生命周期钩子 | Stage 6 |
| 9 | 可观测性与证据层 | 结构化审计日志、指标、因果链追踪 | Stage 4、7 |
| 10 | 安全与治理层 | 动作分级策略引擎、不可信输入处理、指令来源边界 | 贯穿约束（见 README） |
| 11 | 交接与多智能体互操作层 | 交接记录+验证协议、节点身份、子 agent 编排接口 | Stage 7-8 |
| 12 | 配置与扩展层 | 工具动态注册、密钥管理、状态 schema 版本化 | 分布在 Stage 1、4、7 |

---

## Stage 0 — ReAct 核心循环（已完成）

对应文件：[examples/stage2_travel/demo.py](examples/stage2_travel/demo.py)（旅行攻略领域，真实模型，打印每次请求/响应）

已具备：工具定义（JSON Schema）+ 每轮携带、`tool_calls` 解析与回填、`finish_reason`/`stop_reason` 终止判断、
双方言对照（OpenAI/DeepSeek vs Anthropic）、熔断、执行报错回填不 crash、`.env` 密钥加载（配置层的最简版）。

## Stage 1 — 循环通用化（已完成）

把 Stage 0 硬编码的单工具、单模型抽出来，循环机制不再关心具体工具/模型。

- **工具注册表**：`name -> {schema, impl}`，支持**运行时动态注册**（不是写死在代码里，为以后扩展留口子）
- **模型客户端抽象**：统一封装不同厂商方言差异
- **循环核心**：只依赖"注册表 + 客户端"两个抽象

完成标准：换工具集或模型后端，循环代码不用改；新工具能在运行时注册而不改动注册表结构。

## Stage 2 — 目标与任务管理

- ✅ **结构化目标表示**：`Goal` 以 `goal/v1` 数据契约保存目标、成功标准、约束和上下文；循环只接受结构化目标
- ✅ **自然语言目标接入**：把用户原始输入整理为 Goal；缺少关键条件时返回澄清问题，不擅自编造约束
- ✅ **任务分解与状态跟踪**：`TaskPlanner` 让 LLM 提议任务和依赖，Runtime 分配 ID、校验 DAG，并管理任务状态机
- ✅ **任务串行执行**：`TaskExecutor` 选择依赖已满足的任务，交给 ReActLoop 执行，保存结果并更新后续任务状态
- 计划中 **重新规划触发条件**：什么样的新证据出现时，需要回头修改任务分解，而不是硬着头皮按原计划走

当前进度：Stage 2.1–2.4 已完成。自然语言请求可以先整理为结构化 Goal；Goal 会进入 `RunResult` 和证据日志；LLM 可以通过 `TaskPlanner` 提议
任务清单，Runtime 负责生成 `task-plan/v1`、校验依赖和控制 pending/in_progress/completed/failed/blocked
状态转换；`TaskExecutor` 已能串行执行就绪任务、传递直接依赖的结果，并在失败后阻塞下游任务。重新规划尚未实现。

完成标准：给定一个目标，能看到它被拆成的子任务列表和状态变化；中途注入一个改变结论的新事实，能观察到
任务列表被相应调整，而不是被忽略。

## Stage 3 — 多工具编排

- 单轮 `tool_calls` 数组的**并行**处理
- 跨轮的**顺序依赖**处理
- 每个工具调用独立错误处理：单个分支失败返回错误标记，不拖垮整轮

完成标准：一个"查航班 → 查天气 → 建议行程"的真实多步查询，能在若干轮内正确收敛，该并行的并行、该顺序的顺序，
单个工具失败不影响其他并行分支。

## Stage 4 — 持久化状态 / 证据日志（已完成）

- ✅ **Append-only 事件日志**（JSONL）：每个状态转移是不可变事实记录，契约见
  [schemas/v1/event.schema.json](schemas/v1/event.schema.json)
- ✅ **落盘引擎抽象**：`state.Store` 协议 + `JsonlStore`（每次追加 fsync）/ `MemoryStore` 两个实现，
  就是 STRUCTURE.md 里点名的第一个 Rust 接缝
- ✅ **来源标记（provenance）**：每条事件带 `run_id`（哪次运行）、`actor`（intake / planner / executor /
  loop 哪个组件写的）、`caused_by`（触发它的事件 id）。埋点覆盖 intake、planner、任务生命周期和 ReAct 内层循环
- ✅ **会话可恢复**：`replay_run()` 只凭事件序列重建 Goal / TaskPlan / 已完成任务产出，
  `TaskExecutor.resume()` 从重建结果接着跑
- ✅ **状态 schema 版本化**：读到不认识的 `event/vN` 明确抛 `EvidenceSchemaMismatch`，不静默读错

**中断任务的语义**：进程被杀时停在 `in_progress` 的任务，重放保持 `in_progress`（审计要看到真实的最后状态），
续跑时走 `in_progress → failed → pending` 整个重跑。不做半个任务的续跑——ReAct 循环内部没有中途检查点，
重放不出「工具调了一半」的状态。代价是那个任务里已执行的工具会再执行一次；当前工具都是只读查询所以安全，
有副作用的工具要等 Stage 10 策略层来管。

完成标准：跑到一半 kill 进程，重启后正确接着跑；只看日志文件不看代码也能审出会话做了什么；换一个内核版本
读旧日志，能明确识别版本不匹配而不是静默出错。
→ 由 [tests/recovery/test_kill_and_resume.py](packages/kernel/tests/recovery/test_kill_and_resume.py)
（真 SIGKILL 子进程）和 [tests/unit/test_instrumentation.py](packages/kernel/tests/unit/test_instrumentation.py) 守住。

## Stage 5 — Context Engineering 与记忆管理

目标不是单纯把更多文本塞进模型，而是在有限预算内稳定选中完成当前任务所需的信息，同时保住目标、证据、
权限边界和可恢复性。内核只定义通用的数据契约、策略接口和默认实现；代码仓库索引、网页检索、Deep Research
等领域能力作为可插拔 `Retriever` 实现放在内核之外。

### Stage 5A — 上下文契约、装配与预算

- **5.1 `ContextItem` 契约**：所有可进入模型请求的内容统一表示，至少包含 `kind`、`content`、`source`、
  `trust_level`、`priority`、`created_at`、`expires_at`、`reconstructible` 和 `evidence_refs`。模型看到的信息必须能
  说明从哪里来、何时产生、是否可能过期，不能只保存一段无来源文本
- **5.2 `ContextAssembler`**：确定 system / 项目规则 / Goal / 当前 Task / 工具定义 / 对话历史 / 工具结果 /
  检索结果 / 记忆的装配顺序和去重规则；输出统一的 `ContextSnapshot`，模型客户端不再自行拼 request body
- **5.3 `Tokenizer` Protocol + Token 预算**：按 system、tools、goal、history、retrieval、memory 和输出预留分别
  记账；提供标准库默认估算器和厂商精确计数器接缝。装配前做预算，不等 API 返回 context-length 错误
- **5.4 模型能力配置**：上下文窗口、最大输出、Tokenizer、缓存能力由模型配置提供；切换模型时重新计算预算，
  不把某一家模型的限制写死进循环
- **5.5 可恢复快照**：`ContextSnapshot` 带 schema 版本并写入 Stage 4 事件日志；恢复时能从 Goal、Task、事件和
  记忆重建最小有效上下文，不要求把旧会话全文重新发送

### Stage 5B — 检索、压缩、淘汰与缓存

- **5.6 `Retriever` Protocol**：输入当前 Goal / Task / 查询和预算，输出带来源、相关性、时间和证据引用的
  `ContextItem`；支持关键词、结构化过滤和可选语义检索。内核不绑定向量数据库，也不写死代码搜索或网页搜索
- **5.7 检索治理**：限制单来源和总检索预算，去重近似内容；过期、无来源或低可信结果不能覆盖当前观察；记录
  “检索了什么、采用了什么、为什么淘汰”，供回放和评测
- **5.8 `Compressor` Protocol**：旧轮次和大工具结果可以摘要，但摘要必须保留原始证据引用、用户约束、未完成
  动作和错误结论的不确定性；原始内容仍留在情景记忆中，可按引用取回
- **5.9 淘汰策略**：优先淘汰可廉价重建、重复、过期和低相关内容；Goal、当前 Task、用户明确约束、权限状态、
  未决工具调用和关键证据属于受保护内容，不得因为预算不足被静默删除
- **5.10 缓存感知**：固定规则和稳定工具定义尽量保持前缀稳定；压缩优先发生在可变历史尾部；记录 prompt cache
  命中、重复发送 token 和压缩节省量，但不能为了缓存命中牺牲正确性
- **5.11 工具结果分层**：模型窗口只保留结果摘要和证据句柄，完整大结果进入可寻址存储；后续任务能按句柄精确
  取回，避免同一大结果在每轮重复发送

### Stage 5C — 记忆分层与生命周期

- **5.12 记忆分层**：工作记忆（当前窗口，易失）／情景记忆（Stage 4 事件日志，全量存档）／语义记忆
  （从证据提炼的稳定事实，跨 session 常驻）／程序性记忆（用户对“怎么做事”的纠正，影响行为而不是事实）
- **5.13 `MemoryStore` Protocol**：统一写入、读取、检索、失效和来源追踪接口；纯 Python 默认实现可替换为数据库
  或向量索引，调用方不变
- **5.14 写入策略**：只有重复出现、经证据确认、未来仍有价值且不易廉价重建的内容才能晋升为语义/程序性记忆；
  不把每段对话、模型猜测或实时查询结果永久保存
- **5.15 冲突、时效和删除**：当前观察优先于旧记忆；矛盾事实并存时保留版本、时间和来源，不直接覆盖证据；支持
  TTL、显式失效和按用户/会话隔离，避免跨主体串记忆
- **5.16 相关性检索**：综合任务相关性、可信度、新鲜度和历史使用效果排序，而不是只按时间或向量相似度；注入
  模型前仍受本轮 Token 预算约束

### Stage 5D — 信任边界、隐私与质量评测

- **5.17 指令来源分级**：区分 system、项目规则、用户授权、工具观察、外部网页/文件和历史记忆；外部内容默认是
  数据而不是指令，不能通过提示注入提升自己的权限。最终动作授权仍由安全与治理层执行
- **5.18 敏感信息边界**：进入日志、记忆、检索索引和模型请求前使用统一过滤钩子；记录过滤发生过，但不把被过滤
  的秘密再次写进审计 payload
- **5.19 Context 指标**：每次请求记录各类别 token、预算利用率、被压缩/淘汰条目、缓存命中、检索采用率、恢复后
  重建量、成本与延迟，全部关联 Stage 4 的 `run_id` / `caused_by`
- **5.20 任务级评测与回归**：使用固定 fixture 比较不同装配/检索/压缩策略；至少评估关键事实保留率、约束遵守、
  引用可追溯性、过期信息采信、提示注入抵抗、任务成功率、token 成本和延迟，策略变更不能只凭主观观感上线

**分阶段完成标准**：

- **5A**：给定相同 Goal、Task、事件和模型配置，能生成确定、版本化、可解释的 `ContextSnapshot`；超预算时在调用
  模型前明确失败或进入压缩流程；切换两种模型配置后预算正确重算
- **5B**：故意喂入超预算历史和超大工具结果，受保护内容不丢，完整结果可由证据句柄取回；重复请求的稳定前缀
  不被无意义改写；每次检索、压缩和淘汰都能从事件日志解释
- **5C**：跨 session 能取回经证据确认的稳定事实；实时信息不会被永久存死；注入与旧记忆冲突的新观察后，系统
  采用当前观察并保留冲突来源；不同用户/会话的记忆严格隔离
- **5D**：外部网页或工具结果中的恶意指令不能覆盖系统/用户授权；敏感值不进入日志和长期记忆；固定回归集能量化
  Context 策略对成功率、事实保留、token、缓存、成本和延迟的影响

## Stage 6 — 配额感知与检查点

内核只管两件事，"什么时候醒"是外部关注点，不在内核里实现调度器：

- **配额跟踪器**：token / 请求数 / 时间窗口消耗，做成可查询对象，不是暗地记账
- **状态在任意时刻可安全落盘续跑**：不只是配额耗尽时才能存档
- **生命周期钩子**：pre-step / post-step / pre-tool / post-tool / on-error / on-stop，不改核心循环代码
  就能挂扩展逻辑
- **`resume(checkpoint)` 入口**：跟主循环分开，接受外部调度器（cron/任务队列）的调用来恢复执行——内核不
  自己实现"睡眠-唤醒"

完成标准：模拟配额耗尽，循环干净存档退出；外部独立触发 `resume`，能从存档接着跑完剩下的目标；一次跑到一半
的任意时刻手动触发存档，同样能正确恢复（不局限于配额耗尽这一种触发场景）。

## Stage 7 — 可验证交接

- **交接记录 schema**：目标状态 + 证据引用（指回 Stage 4 事件日志）+ 完整性校验（hash）+ 节点身份 +
  下一步建议 + **待人类确认动作标记**（涉及花钱/发消息等后果的步骤，必须显式标出还没拿到确认）
- **依赖 Stage 1**：模型客户端必须支持 schema 约束的结构化输出，交接记录不能靠指望模型在文字里吐出
  格式正确的 JSON
- **可验证 ≠ 完整性校验**：hash 只证明交接记录没被篡改，不证明记录结论有证据支撑——真正的可验证是接收方
  能仅凭 Stage 4 的事件日志独立复算出交接记录里的结论
- **状态 schema 版本化**：交接记录格式同样带版本号

完成标准：一个 loop 实例把状态交给另一个独立进程/session，接收方不信任发送方、只信任事件日志，也能验证
完整性并从交接点正确续接；带"待确认动作"的交接记录，接收方能明确看到哪些步骤不能自动往下走。

## Stage 8（内核之外，暂不做）— 图编排层

并发拉起多个 kernel 实例（planner / worker / synthesizer / reviewer 等角色），Gate 按节点身份把失败路由回
具体那个节点，而不是整图重跑。依赖 Stage 4 的来源标记 + Stage 7 的节点身份，不需要发明新的执行模型。
暂不启动：没有至少 2-3 个真实节点角色之前，没有东西可并行，提前做是过度设计。
