# Loopbase — 路线图

Stage 0-7 是内核要做的部分，Stage 8 是内核之外的编排层，暂不启动。整体定位、范围边界和设计原则见
[README.md](README.md)。

## 模块全景图

12 个模块层，每层标注对应哪个 Stage：

| # | 层 | 职责 | 对应 Stage |
|---|---|---|---|
| 1 | 核心循环控制层 | turn 执行器、终止判定、分支/并行控制原语、错误边界 | Stage 0-3 |
| 2 | 模型 I/O 层 | 多模型客户端抽象、结构化输出强制、流式增量拼装、transient 失败重试 | Stage 1 |
| 3 | 工具/动作层 | 工具注册表（含动态注册）、执行沙箱/权限边界、并发调度、结果归一化 | Stage 1、3 |
| 4 | 目标与任务管理层 | 结构化目标表示、任务分解与状态跟踪、重新规划触发条件 | Stage 2 |
| 5 | 上下文管理层 | 装配、预算跟踪、压缩、淘汰、缓存感知压缩 | Stage 5 |
| 6 | 记忆层 | 工作/情景/语义/程序性记忆、写入策略、冲突消解、检索 | Stage 5 |
| 7 | 状态持久层 | 事件日志、检查点/恢复协议、多会话身份隔离、状态 schema 版本化 | Stage 4 |
| 8 | 调度与生命周期层 | 配额跟踪、唤醒契约（外部调度器驱动）、生命周期钩子 | Stage 6 |
| 9 | 可观测性与证据层 | 结构化审计日志、指标、因果链追踪 | Stage 4、7 |
| 10 | 安全与治理层 | 动作分级策略引擎、不可信输入处理、指令来源边界 | 贯穿约束（见 README） |
| 11 | 交接与多智能体互操作层 | 交接记录+验证协议、节点身份、子 agent 编排接口 | Stage 7-8 |
| 12 | 配置与扩展层 | 工具动态注册、密钥管理、状态 schema 版本化 | 分布在 Stage 1、4、7 |

---

## Stage 0 — ReAct 核心循环（已完成）

对应文件：[examples/stage2_finance/demo.py](examples/stage2_finance/demo.py)（金融领域，真实模型，打印每次请求/响应）

已具备：工具定义（JSON Schema）+ 每轮携带、`tool_calls` 解析与回填、`finish_reason`/`stop_reason` 终止判断、
双方言对照（OpenAI/DeepSeek vs Anthropic）、熔断、执行报错回填不 crash、`.env` 密钥加载（配置层的最简版）。

## Stage 1 — 循环通用化（已完成）

把 Stage 0 硬编码的单工具、单模型抽出来，循环机制不再关心具体工具/模型。

- **工具注册表**：`name -> {schema, impl}`，支持**运行时动态注册**（不是写死在代码里，为以后扩展留口子）
- **模型客户端抽象**：统一封装不同厂商方言差异
- **循环核心**：只依赖"注册表 + 客户端"两个抽象

完成标准：换工具集或模型后端，循环代码不用改；新工具能在运行时注册而不改动注册表结构。

## Stage 2 — 目标与任务管理

- **结构化目标表示**：当前目标是数据，不是隐含在 system prompt 里的一句话
- **任务分解与状态跟踪**：拆成子任务，每个子任务有 pending/in_progress/completed 状态
- **重新规划触发条件**：什么样的新证据出现时，需要回头修改任务分解，而不是硬着头皮按原计划走

完成标准：给定一个目标，能看到它被拆成的子任务列表和状态变化；中途注入一个改变结论的新事实，能观察到
任务列表被相应调整，而不是被忽略。

## Stage 3 — 多工具编排

- 单轮 `tool_calls` 数组的**并行**处理
- 跨轮的**顺序依赖**处理
- 每个工具调用独立错误处理：单个分支失败返回错误标记，不拖垮整轮

完成标准：一个"查航班 → 查天气 → 建议行程"的真实多步查询，能在若干轮内正确收敛，该并行的并行、该顺序的顺序，
单个工具失败不影响其他并行分支。

## Stage 4 — 持久化状态 / 证据日志

- **Append-only 事件日志**（JSONL）：每个状态转移是不可变事实记录
- **会话可恢复**：从磁盘重新加载续跑
- **来源标记（provenance）**：每条事件记录谁产生的、依据什么调用的
- **状态 schema 版本化**：日志/检查点格式带版本号，内核升级后旧文件不会被新代码悄悄读错

完成标准：跑到一半 kill 进程，重启后正确接着跑；只看日志文件不看代码也能审出会话做了什么；换一个内核版本
读旧日志，能明确识别版本不匹配而不是静默出错。

## Stage 5 — 上下文与记忆管理

- **5.1 上下文装配**：system prompt + 工具清单 + 历史怎么组装成 request body
- **5.2 Token 预算跟踪**：system / tools / history / 输出预留分别记账
- **5.3 压缩/摘要**：旧轮次摘要成精简版本，而不是直接截断
- **5.4 淘汰策略**：大段工具返回结果优先于对话文本被摘要/截断
- **5.5 缓存感知压缩**：压缩策略保住 prompt 前缀稳定，避免 KV cache miss
- **5.6 记忆分层**：工作记忆（当前窗口，易失）／情景记忆（Stage 4 的事件日志，全量存档）／语义记忆
  （从情景记忆提炼的稳定事实，跨 session 常驻）／程序性记忆（"怎么做事"的纠正记录，影响行为不是事实）
- **5.7 记忆写入策略**：什么条件触发把情景记忆提炼进语义记忆，不是所有东西都值得往上提
- **5.8 冲突消解**：新观察和旧记忆矛盾时，以当前观察为准覆盖旧结论，不盲信历史记忆
- **5.9 检索**：语义/程序性记忆按相关性而非时间检索回填
- **5.10 存储边界**：能从当前状态廉价重新推导出来的东西不进语义记忆（比如实时航班状态不该被当成
  记住的"事实"存死）

完成标准：故意喂超预算的对话，压缩后关键事实靠语义记忆兜底不丢；重复请求 KV cache 命中率没被压缩策略破坏；
故意注入一条与旧记忆矛盾的新事实，验证系统采信新的而不是旧的。

## Stage 6 — 配额感知与检查点

内核只管两件事，"什么时候醒"是外部关注点，不在内核里实现调度器：

- **配额跟踪器**：token / 请求数 / 时间窗口消耗，做成可查询对象，不是暗地记账
- **状态在任意时刻可安全落盘续跑**：不只是配额耗尽时才能存档
- **生命周期钩子**：pre-step / post-step / pre-tool / post-tool / on-error / on-stop，不改核心循环代码
  就能挂扩展逻辑
- **`resume(checkpoint)` 入口**：跟主循环分开，接受外部调度器（cron/任务队列）的调用来恢复执行——内核不
  自己实现"睡眠-唤醒"

完成标准：模拟配额耗尽，循环干净存档退出；外部独立触发 `resume`，能从存档接着跑完剩下的目标；一次跑到一半
的任意时刻手动触发存档，同样能正确恢复（不局限于配额耗尽这一种触发场景）。

## Stage 7 — 可验证交接

- **交接记录 schema**：目标状态 + 证据引用（指回 Stage 4 事件日志）+ 完整性校验（hash）+ 节点身份 +
  下一步建议 + **待人类确认动作标记**（涉及花钱/发消息等后果的步骤，必须显式标出还没拿到确认）
- **依赖 Stage 1**：模型客户端必须支持 schema 约束的结构化输出，交接记录不能靠指望模型在文字里吐出
  格式正确的 JSON
- **可验证 ≠ 完整性校验**：hash 只证明交接记录没被篡改，不证明记录结论有证据支撑——真正的可验证是接收方
  能仅凭 Stage 4 的事件日志独立复算出交接记录里的结论
- **状态 schema 版本化**：交接记录格式同样带版本号

完成标准：一个 loop 实例把状态交给另一个独立进程/session，接收方不信任发送方、只信任事件日志，也能验证
完整性并从交接点正确续接；带"待确认动作"的交接记录，接收方能明确看到哪些步骤不能自动往下走。

## Stage 8（内核之外，暂不做）— 图编排层

并发拉起多个 kernel 实例（planner / worker / synthesizer / reviewer 等角色），Gate 按节点身份把失败路由回
具体那个节点，而不是整图重跑。依赖 Stage 4 的来源标记 + Stage 7 的节点身份，不需要发明新的执行模型。
暂不启动：没有至少 2-3 个真实节点角色之前，没有东西可并行，提前做是过度设计。
